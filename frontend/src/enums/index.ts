export enum Constants {
  TOKEN = "token",
}

export enum RoutePath {
  DASHBOARD = "/dashboard",
  KYC_FORM = "/kyc-form",
  REGISTER = "/register",
  LOGIN = "/login",
  PROFILE = "/profile",
}

export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
  SUPER = "SUPER",
}
